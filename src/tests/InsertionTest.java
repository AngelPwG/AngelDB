package tests;

import btree.BPlusTree;
import models.Row;

public class InsertionTest {
    public static void main(String[] args) {
        /*
        BPlusTree tree = new BPlusTree(40);

        System.out.println("Starting Insertions");

        for (int i = 1; i <= 1000; i++){
            long key = i * 10;
            System.out.println("Inserting: " + key);
            tree.insert(key, new Row(key, "User " + key, (int)(Math.random() * 10) + 7));
        }

        System.out.println("Testing Search");

        Row found = tree.search(50);
        System.out.println("Search 50: " + ((found != null) ? "FOUND " + found : "NOT FOUND"));

        Row missing = tree.search(55);
        System.out.println("Search 55: " + ((missing != null) ? "FOUND " + found : "NOT FOUND"));

        tree.printStructure();
         */
    }
}
