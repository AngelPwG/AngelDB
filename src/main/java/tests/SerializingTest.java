package tests;

public class SerializingTest {

    public static void main(String[] args) {
        //LeafNode node = new LeafNode(40);
        //node.keys.add(1L);
        //node.data.add(new Row(1L, "Jose Angel", 20));
        //DiskManager diskManager = new DiskManager();
        //byte[] data = diskManager.serializeLeaf(node);
        //LeafNode restoredNode = diskManager.deserializeLeaf(data);
//
        //System.out.println(restoredNode.data.get(0));
    }
}
